[user]
	email = david@mandelberg.org
	name = David Mandelberg

[alias]
	# TODO: Once aliases can call other aliases, reduce duplication:
	# https://github.com/git/git/commit/506ee60d222f94a1598769feaba3b3621bd9fa1a

	# log
	review = log --pretty=fuller -p --reverse
	rin = log --pretty=fuller -p --reverse ..@{upstream}
	rout = log --pretty=fuller -p --reverse @{push}..
	graph = log --pretty=shorter --graph --boundary
	gin = log --pretty=shorter --graph --boundary ..@{upstream}
	gout = log --pretty=shorter --graph --boundary @{push}..
	ginout = "! \
		: log && \
		cd \"${GIT_PREFIX}\" && \
		all=\"@{push} @{upstream} HEAD\" && \
		bases=\"$(git merge-base --all --octopus ${all})\" && \
		git graph ${all} --not ${bases}"

	# commit
	amend = commit --amend -C HEAD
	reword = commit --amend -c HEAD --only

[pretty]
	shorter = tformat:%C(auto)%h %ae %C(blue)%ar%C(auto)%d%n%s

[color]
	ui = true

[diff]
	mnemonicPrefix = true
	renames = true
	algorithm = histogram
	colorMoved = true

[status]
	showStash = true

[push]
	default = upstream

[merge]
	conflictStyle = diff3

[rebase]
	autoSquash = true
	missingCommitsCheck = error

[core]
	logAllRefUpdates = always

[transfer]
	fsckObjects = true

[include]
	path = config.local

[alias]
	# log
	r = log --pretty=fuller --stat -p --reverse
	rin = r ..@{upstream}
	rout = r @{push}..
	g = log --pretty=shorter --graph --boundary
	gin = g ..@{upstream}
	gout = g @{push}..
	ginout = "! \
		: log && \
		cd \"${GIT_PREFIX}\" && \
		all=\"@{push} @{upstream} HEAD\" && \
		bases=\"$(git merge-base --all --octopus ${all})\" && \
		git g ${all} --not ${bases}"

	# commit
	amend = commit --reset-author --amend -C HEAD
	reword = commit --reset-author --amend -c HEAD --only

	# diff
	d = diff

	# status
	st = status

	# push
	fpush = push --force-with-lease

[color]
	ui = true

[core]
	logAllRefUpdates = always

[diff]
	mnemonicPrefix = true
	renames = true
	algorithm = histogram
	colorMoved = true

[init]
	defaultBranch = main

[merge]
	conflictStyle = diff3

[pretty]
	shorter = tformat:%C(auto)%h %ae %C(blue)%ar%C(auto)%d%n%s

[push]
	default = upstream

[rebase]
	autoSquash = true
	missingCommitsCheck = error

[revert]
	reference = true

[status]
	showStash = true

[transfer]
	fsckObjects = true

[url "ssh://git@github.com/"]
	pushInsteadOf = https://github.com/

[url "ssh://git@gitlab.com/"]
	pushInsteadOf = https://gitlab.com/

[url "ssh://git@gitlab.freedesktop.org/"]
	pushInsteadOf = https://gitlab.freedesktop.org/

[url "ssh://git@salsa.debian.org/"]
	pushInsteadOf = https://salsa.debian.org/

[url "ssh://git@ssh.gitlab.gnome.org/"]
	pushInsteadOf = https://gitlab.gnome.org/

[user]
	email = david@mandelberg.org
	name = David Mandelberg

[include]
	path = config.local
